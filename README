应用包含五个核心视图，通过底部标签页切换：

全部：展示词库中所有尚未标记为“熟记”的生词。
熟记：已掌握的单词，自动移出“全部”与“不熟”的常规复习队列。
不熟：标记为掌握不佳的单词，便于集中突破。
重要：被添加星标的单词（与熟练度状态独立）。
历史：按时间倒序排列的最近浏览记录。点击列表中的单词可跳转至该词卡片。



在单词卡片界面，可进行以下交互：

1. 点击底部按钮更新当前单词状态（“熟记”与“不熟”状态互斥）。状态变更后，系统会自动清洗队列，确保列表内单词始终符合当前分类规则。
2. 直接点击卡片顶部的大写单词即可触发朗读。展示某个单词时也会自动播放朗读。
3. 当前分类下的单词复习完毕时，系统会自动提示并打乱顺序，开启新一轮循环。



支持键盘和移动端手势操作，以提升复习效率：

功能			键盘快捷键					移动端手势
下一个单词		Space 或 键盘上的右箭头		向左滑动卡片
上一个单词		键盘上的左箭头				向右滑动卡片
标记熟记		1							-
标记不熟		2							-
标记重要		3							-
发起纠错		C							-
撤销纠错		Z							-
关闭当前弹窗	Esc							-



遇到释义缺失或排版错误时，点击右上角“纠错”按钮。在输入框中用自然语言描述问题（如：“数量级的英文释义缺失”），系统会调用大模型自动修正底层数据并实时刷新卡片。

如果该单词曾经被纠错过，右上角会出现黄色的“撤销”按钮。点击可对比修改前后的数据，并一键恢复到原始状态。


启动方式：

1. 在 RollingWord 项目的根目录执行：python scripts/import_to_sqlite.py
这将会根据 data.json 的内容自动化生成项目所需的数据库文件

2. 在 RollingWord 项目的根目录执行：python backend/server.py